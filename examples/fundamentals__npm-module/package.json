{
  "name": "cypress-npm-module",
  "version": "1.0.0",
  "description": "Run Cypress via its NPM module API",
  "scripts": {
    "cypress:run": "node ./e2e-tests",
    "cypress:open": "../../node_modules/.bin/cypress open",
    "test:ci:windows": "node ./e2e-tests"
  }
}
