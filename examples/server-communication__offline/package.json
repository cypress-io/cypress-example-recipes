{
  "name": "offline",
  "version": "1.0.0",
  "description": "Offline network test",
  "private": true,
  "scripts": {
    "cypress:open": "../../node_modules/.bin/cypress open",
    "cypress:run": "../../node_modules/.bin/cypress run --browser chrome",
    "cypress:run:record": "../../node_modules/.bin/cypress run --record --browser chrome",
    "dev": "../../node_modules/.bin/start-test 7080 cypress:open",
    "start": "../../node_modules/.bin/serve -l 7080 --no-clipboard"
  }
}
